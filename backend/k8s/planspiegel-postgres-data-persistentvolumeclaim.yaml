apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: planspiegel-postgres-data
  name: planspiegel-postgres-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
