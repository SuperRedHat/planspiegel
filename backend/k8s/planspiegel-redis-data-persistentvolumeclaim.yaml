apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: planspiegel-redis-data
  name: planspiegel-redis-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
